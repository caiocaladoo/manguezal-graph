'use strict';

var main = (function(){

      graphDictionary.init();




      function init(){
            console.log(graphDictionary.getNodeClass("caio"));
            graph.init();
      }

      init();

      return {
            init : init
      };

})();
